{
  "name": "gulandamsite",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "sass:watch": "sass --watch src/styles:public --style compressed",
    "sass:build": "sass src/styles:public --style compressed",
    "postcss": "postcss public/styles.css --use autoprefixer -r",
    "build": "npm run sass:build && npm run postcss",
    "watch": "npm run sass:watch",
    "serve": "live-server",
    "start": "npm run sass:watch"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "autoprefixer": "^10.4.22",
    "postcss-cli": "^11.0.1",
    "sass": "^1.94.2",
    "webpack": "^5.103.0",
    "webpack-cli": "^6.0.1"
  }
}
